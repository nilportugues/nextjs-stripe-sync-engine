

async function connectAndMigrate(
  databaseUrl: string | undefined,
  migrationsDirectory: string,
  logOnError = false
) {
 console.log("no needed, remove this method call from everywhere");
}

export async function runMigrations(): Promise<void> {
  console.log("no needed, remove this method call from everywhere");
}
